# 桌宠 - 打包说明

## 📦 打包方式

### 方式一：GitHub Actions 自动构建（推荐）

使用 GitHub Actions 可以同时构建 Windows (`.exe`) 和 macOS (`.app`) 两个版本。

#### 步骤：

1. **创建 GitHub 仓库**
   - 将此项目上传到 GitHub

2. **触发构建**
   - 方式 A：打标签触发
     ```bash
     git tag -a v1.0.0 -m "Release v1.0.0"
     git push origin v1.0.0
     ```
   
   - 方式 B：手动触发
     - 到 GitHub 仓库的 Actions 页面
     - 选择 "Build Desktop App" workflow
     - 点击 "Run workflow"

3. **下载构建产物**
   - 构建完成后，在 Releases 页面下载：
     - `DesktopPet-Windows.zip` - Windows 版本
     - `DesktopPet-macOS.dmg` - macOS 版本

---

### 方式二：本地打包（仅限 macOS）

在 Mac 上只能打包出 `.app` 格式，无法打包 `.exe`。

```bash
# 直接运行打包脚本
./build.sh
```

或者手动执行：

```bash
# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 打包
pyinstaller --clean desktop_pet.spec

# 输出在 dist/桌宠.app
```

---

## 📂 文件说明

| 文件 | 说明 |
|------|------|
| `desktop_pet.spec` | PyInstaller 配置文件 |
| `build.sh` | macOS 本地打包脚本 |
| `.github/workflows/build.yml` | GitHub Actions 自动构建配置 |
| `requirements.txt` | Python 依赖列表 |
